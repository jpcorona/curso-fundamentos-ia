# Ejemplos heredados · SDK 1.x/classic

Estos archivos se conservan como referencia histórica. Usan `create_agent`, hilos y runs de Agents classic; no son compatibles con el entorno principal basado en `azure-ai-projects` 2.4.0.

Para ejecutar el curso, sigue el README de la raíz y los materiales actualizados. No instales estas dependencias en el mismo entorno virtual del SDK 2.x.

